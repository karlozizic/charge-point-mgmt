# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# Only variables that are actually consumed by the code are listed here. If you add
# one, add it here too — an undocumented variable is a variable someone will forget.

# --- PostgreSQL (docker-compose) -------------------------------------------
# Local throwaway container. Not a production secret, but keep one source of truth:
# docker-compose.yml and the app connection string both read from here.
POSTGRES_DB=cpms
POSTGRES_USER=cpmsuser
POSTGRES_PASSWORD=cpms_local_dev

# --- pgAdmin (docker-compose, optional) ------------------------------------
PGADMIN_EMAIL=admin@cpms.local
PGADMIN_PASSWORD=cpms_local_dev

# --- Application connection string ------------------------------------------
# Consumed as ConnectionStrings:MartenDb. Overrides appsettings.
ConnectionStrings__MartenDb=Host=localhost;Port=5432;Database=cpms;Username=cpmsuser;Password=cpms_local_dev

# --- Stripe -----------------------------------------------------------------
# Read in CPMS.API/Program.cs and Controllers/WebhookController.cs.
# Test-mode keys only. The secret key must never appear in any tracked file.
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET_PROD=whsec_...

# --- Frontend (Vite build-time) ---------------------------------------------
# Baked into the JS bundle at build time and therefore public by design.
# Restrict it by URL in the Mapbox dashboard rather than treating it as a secret.
VITE_MAPBOX_ACCESS_TOKEN=pk....
